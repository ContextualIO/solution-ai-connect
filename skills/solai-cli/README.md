# solai-cli

Portable shell-first skill for working with the local `ctxl` CLI from coding agents.

This repository is designed to work in two ways:

- installed directly as a standalone skill in a shell-capable coding agent
- included as a submodule inside a Claude plugin or similar bundle at `skills/solai-cli/`

The skill expects `ctxl` to already be installed on the machine.
